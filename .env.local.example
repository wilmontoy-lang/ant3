# ── Variables de entorno para Vercel ──────────────────────────────
# Solo esta variable es necesaria para el deploy básico.
# Supabase es opcional (activa funciones avanzadas como admin y funciones de cargos).

# PIN del panel de administración (Admin > editar cargos)
VITE_ADMIN_PIN=tu_pin_aqui

# ── Opcional: Supabase (habilita overrides admin + tab Funciones) ─
# VITE_SUPABASE_URL=https://xxxxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhb...
